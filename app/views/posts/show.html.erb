<div id="post-wrapper">
	<h1 id="post-title">BOARDS</h1>
  <div id="post"><p><%= @post.title %></p>
  <div id="post"><p><%= @post.content %></p>

	<%= form_with(model: @comment, local: true) do |c| %>
    
    <div><%=c.text_area :body%></div>
     <div><%= c.hidden_field :post_id, :value => @post.id %></div>
   
    <div><%=c.submit %></div>

    <% end %>


<% @comments.each do |c| %>

    <div id="post"><%= @user.email %>:said
    <div id="post"><p><a href='/comments/<%= c.id %>'><%= c.body %></a></p></div>
    	
    	<div><%= link_to 'Delete', "/comments/#{c.id}", method: :delete %></div>
    

    



<%end%>	

</div>
</div>
</div>
</div>
</div>